import { FC } from 'react';
import { calendarLocal } from '../../types/type';
interface ITodayBtn {
    local: calendarLocal;
    todayBtn: boolean;
}
export declare const TodayBtn: FC<ITodayBtn>;
export {};
