import { IDay } from '../../types/type';
export declare const checkInputValues: (initValue: any, correctedLocal: string, correctedType: string, maxDate?: IDay, minDate?: IDay, disabledDates?: IDay[]) => void;
